import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'BetterDevs - Elevate your dev skills',
  description:
    'Are you a passionate developer, but still have a lot to learn? Gain access to best resources to skyrocket your skills in Typescript, React, Node.js and more'
}

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
